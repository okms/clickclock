{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window: window show/hide/focus, events, autostart, and opening the project's release page (UPD-04). No fs, shell, or updater access; no unrestricted http or opener access (NF-01).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "autostart:default",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "https://github.com/okms/clickclock/*" }]
    }
  ]
}
